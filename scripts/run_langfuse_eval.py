"""Runs Langfuse-backed evaluation metrics (prompt quality, retrieval scores, cost trends).

Deferred to Phase 13 — requires the LangGraph agent graph (Phase 7), Langfuse
tracing integration, and the evaluation module (Phase 13) to be implemented first.

Usage (Phase 13+):
    python scripts/run_langfuse_eval.py
"""

if __name__ == "__main__":
    raise NotImplementedError(
        "run_langfuse_eval.py is deferred to Phase 13. "
        "Implement the agent graph (Phase 7) and Langfuse tracing first."
    )
