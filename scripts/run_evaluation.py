"""Runs the full agent evaluation suite against data/evaluation/eval_cases.json.

Deferred to Phase 13 — requires the LangGraph agent graph (Phase 7) and
FastAPI backend (Phase 10) to be implemented first.

Usage (Phase 13+):
    python scripts/run_evaluation.py
    python scripts/run_evaluation.py --cases eval_cases.json
"""

if __name__ == "__main__":
    raise NotImplementedError(
        "run_evaluation.py is deferred to Phase 13. "
        "Implement the agent graph (Phase 7) and FastAPI backend (Phase 10) first."
    )
